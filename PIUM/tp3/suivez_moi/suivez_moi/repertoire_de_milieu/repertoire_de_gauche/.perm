if [ -e ./tour_de_guet1 ] || [ -e ./tour_de_guet2 ]; then
     echo
     echo "Pour pouvoir entrer dans la garnison, il faut d'abord détruire les"
     echo "deux tours de guet"
     echo
else
	chmod a+rwx garnison
	echo
        echo "Ok, le chemin est ouvert."
	echo
fi